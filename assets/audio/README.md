Radio audio lives here.

Use only audio you own, created yourself, or have permission to use. The station `src` values in `js/main.js` can point to these local files or to direct HTTPS audio stream URLs.
